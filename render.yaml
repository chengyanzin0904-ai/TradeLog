services:
  - type: web
    name: trade-journal-pipeline
    env: docker
    plan: starter
    disk:
      name: trade-journal-data
      mountPath: /app/data
      sizeGB: 1
    envVars:
      - key: APP_DATA_DIR
        value: /app/data
